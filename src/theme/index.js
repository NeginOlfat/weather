import { color } from "./color";
import { space } from "./space";
import { size } from "./size";
import { font, fontSize } from "./font";

export const theme = {
    color,
    space,
    size,
    font,
    fontSize
};
